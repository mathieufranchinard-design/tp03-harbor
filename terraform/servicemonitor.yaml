apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: harbor-metrics
  namespace: harbor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: harbor
  endpoints:
    - port: http-metrics
      path: /metrics
      interval: 30s
